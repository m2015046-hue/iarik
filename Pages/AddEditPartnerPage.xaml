<Page x:Class="PolApp.Pages.AddEditPartnerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddEditPartnerPage">

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0" Text="Тип партнера" VerticalAlignment="Center" Margin="10 6"/>
        <ComboBox x:Name="TypeBox"
          Grid.Column="1" Grid.Row="0"
          SelectedItem="{Binding Partners_type, Mode=TwoWay}"
          DisplayMemberPath="typePartner"
          Margin="10 0 100 0" Height="40"/>

        <TextBlock Grid.Column="0" Grid.Row="1" Text="Название партнера" VerticalAlignment="Center" Margin="10 6"/>
        <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding namePartner}" Margin="10 6 100 6"/>

        <TextBlock Grid.Column="0" Grid.Row="2" Text="Email" VerticalAlignment="Center" Margin="10 6"/>
        <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding email}" Margin="10 6 100 6"/>

        <TextBlock Grid.Column="0" Grid.Row="3" Text="Телефон" VerticalAlignment="Center" Margin="10 6"/>
        <TextBox Grid.Column="1" Grid.Row="3" Text="{Binding phone}" Margin="10 6 100 6"/>

        <TextBlock Grid.Column="0" Grid.Row="4" Text="Адрес" VerticalAlignment="Center" Margin="10 6"/>
        <TextBox Grid.Column="1" Grid.Row="4" Text="{Binding adress}" Margin="10 6 100 6"/>

        <TextBlock Grid.Column="0" Grid.Row="5" Text="ИНН" VerticalAlignment="Center" Margin="10 6"/>
        <TextBox Grid.Column="1" Grid.Row="5" Text="{Binding inn}" Margin="10 6 100 6"/>

        <TextBlock Grid.Column="0" Grid.Row="6" Text="Рейтинг" VerticalAlignment="Center" Margin="10 6"/>
        <TextBox Grid.Column="1" Grid.Row="6" Text="{Binding rate}" Margin="10 6 100 6"/>

        <TextBlock Grid.Column="0" Grid.Row="7" Text="Фамилия директора" VerticalAlignment="Center" Margin="10 6"/>
        <TextBox Grid.Column="1" Grid.Row="7" Text="{Binding surnameDirector}" Margin="10 6 100 6"/>

        <TextBlock Grid.Column="0" Grid.Row="8" Text="Имя директора" VerticalAlignment="Center" Margin="10 6"/>
        <TextBox Grid.Column="1" Grid.Row="8" Text="{Binding nameDirector}" Margin="10 6 100 6"/>

        <TextBlock Grid.Column="0" Grid.Row="9" Text="Отчество директора" VerticalAlignment="Center" Margin="10 6"/>
        <TextBox Grid.Column="1" Grid.Row="9" Text="{Binding patronymicDirector}" Margin="10 6 100 6"/>

        <Button x:Name="SaveButton"
                Content="Сохранить"
                Click="SaveButton_Click"
                Grid.ColumnSpan="2"
                Grid.Row="10"
                HorizontalAlignment="Center"
                Margin="10"/>
    </Grid>
</Page>