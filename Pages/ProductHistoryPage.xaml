<Page x:Class="PolApp.Pages.ProductHistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PolApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductHistoryPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <DataGrid Name="HistoryDataGrid" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Артикул" Binding="{Binding idArticle}"/>
                <DataGridTextColumn Header="Название продукции" Binding="{Binding Product.nameProduct}"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding countProduct}"/>
                <DataGridTextColumn Header="Дата продажи" Binding="{Binding dateOfSale, StringFormat={}{0:dd.MM.yyyy}}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock Name="CountText" Grid.Row="1"/>
    </Grid>
</Page>
