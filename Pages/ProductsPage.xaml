<Page x:Class="PolApp.Pages.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PolApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductsPage">

    <Grid>
        <ListBox x:Name="ListBoxProducts" Background="#f5f5f5" ScrollViewer.VerticalScrollBarVisibility="Visible" HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Background="White" Margin="5">
                        <StackPanel Margin="5">
                            <StackPanel Orientation="Horizontal"  Margin="5">
                                <TextBlock Text="Артикул:" FontSize="14"></TextBlock>
                                <TextBlock FontSize="14" Text="{Binding Path=idArticle, StringFormat={}{0}}" Margin="5,0"/>
                            </StackPanel>
                            <TextBlock Margin="5" FontSize="20" Text="{Binding Path=nameProduct}" TextWrapping="Wrap" Width="Auto" HorizontalAlignment="Left"/>
                            <StackPanel Orientation="Horizontal" Margin="5">
                                <TextBlock Text="Тип:" FontSize="14"></TextBlock>
                                <TextBlock FontSize="14" Text="{Binding Path=Product_type.typeProduct, StringFormat={}{0} штук}" Margin="5,0"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="5">
                                <TextBlock Text="Стоимость:" FontSize="14"></TextBlock>
                                <TextBlock FontSize="14" Text="{Binding Path=price, StringFormat={}{0} рублей}" Margin="5,0"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>

